/**
 * OWNER: Member 5 (Offline, Print & Share).
 * Lists packs held in IndexedDB, with an offline-verified marker per pack.
 */
export function SavedPacks() {
  // TODO(M5): read from offline/store.ts, show savedAt + verifiedOffline
  return <p>SavedPacks — Member 5</p>;
}
