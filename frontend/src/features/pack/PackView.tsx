/**
 * OWNER: Member 4 (Prep Flow), with Member 5 on the print and save paths.
 *
 * Eight sections in this fixed order. The order is the product — she reads the
 * briefing before the plan, because understanding comes before delivery:
 *   1 briefing  2 why  3 spine  4 board  5 activity  6 questions  7 assessment  8 verify
 *
 * Every section renders a provenance chip (from design / generated). That chip is
 * not decoration — it is the only defence a teacher has against a confident error.
 */
export function PackView() {
  // TODO(M4): load from IndexedDB first, fall back to the network. Offline is the
  // default case in this room, not the error case.
  return <p>PackView — Member 4</p>;
}
