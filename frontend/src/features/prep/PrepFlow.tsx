/**
 * OWNER: Member 4 (Prep Flow).
 *
 * The whole flow is: what am I teaching → what do I have → build the pack.
 * Target is three taps from open to pack. Every extra field costs a tap, and she
 * is doing this at 9pm.
 */
export function PrepFlow() {
  // TODO(M4): subject → strand → sub-strand pickers (api.subjects / .strands / .subStrands)
  // TODO(M4): context form — class size, lesson minutes, language, resource level
  // TODO(M4): submit via api.createPack, show real progress (it takes ~20-40s),
  //           then navigate to /pack/:id
  return <p>PrepFlow — Member 4</p>;
}
