# handoff-prompt.md — Builder instructions, Sprint 003

Execute the approved plan (this pack's blueprint.md summarizes it). Checkpoints CP-A → CP-D, one commit each (`sprint3 checkpoint X: ...`). Hold: R1 (no learner data — analytics stay aggregate/self-scoped), DEC-009 (badges), DEC-010 (fixtures cover everything; E2E must run keyless). Keep CSS variable NAMES stable when swapping tokens. Record DEC-019..024 in planning/DECISIONS.md at CP-D. Cut order if behind: voice quiz read-aloud → notes export → revisit radar → demo one-click (fall back to displayed credentials).
